1. Hola tengo un proyecto con la siguiente estructura de carpetas:

|--backend/
|--database/
|  |--init.sql
|--frontend/
|--.env
|--.env.example
|--.gitignore
|--docker-compose.yml
|--README.md

2. Dentro de la carpeta backend/ tengo esta estructura:

|--api-shifts/
|  |--bin/
|  |--Controllers/
|  |--Data/
|  |--Dtos/
|  |--Models/
|  |--obj/
|  |--Properties/
|  |--api-shifts.csproj
|  |--api-shifts.http
|  |--appsettings.Development.json
|  |--appsettings.json
|  |--Dockerfile
|  |--Program.cs
|  |--WeatherForecast.cs
|--api-shifts.sln
|--.dockerignore

El backend esta hecho con .net core 8

Dockerfile de apishifts:

FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
USER app
WORKDIR /app
EXPOSE 8080
EXPOSE 8081

FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
ARG BUILD_CONFIGURATION=Release
WORKDIR /src
COPY ["api-shifts/api-shifts.csproj", "api-shifts/"]
RUN dotnet restore "./api-shifts/api-shifts.csproj"
COPY . .
WORKDIR "/src/api-shifts"
RUN dotnet build "./api-shifts.csproj" -c $BUILD_CONFIGURATION -o /app/build

FROM build AS publish
ARG BUILD_CONFIGURATION=Release
RUN dotnet publish "./api-shifts.csproj" -c $BUILD_CONFIGURATION -o /app/publish /p:UseAppHost=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "api-shifts.dll"]

3. Dentro de la carpeta frontend/ tengo esta estructura:

|--frontend/
|  |--.next/
|  |--node_modules/
|  |--public/
|  |--src/
|  |--.eslintrc.json
|  |--.gitignore
|  |--components.json
|  |--Dockerfile
|  |--next-env.d.ts
|  |--next.config.mjs
|  |--package-lock.json
|  |--package.json
|  |--postcss.config.mjs
|  |--README.md
|  |--tailwind.config.ts
|  |--tsconfig.json

El frontend esta hecho con NextJS usando la libreria de comoponentes shadcn

4. PETICION: Necesito que hagas los Dockerfile correspondiente a cada carpeta, los recursos que voy a necesitar es el backend, el frontend y la base de datos, la base de datos debe ser en sql server y las tablas se deben generar al iniciar el proyecto con docker compose usando el archivo init.sql que esta dentro de la carpeta database

5. VALORES ADICIONALES QUE PUEDEN SERVIR:

appsettings.json de api-shifts/  :

{
  "ConnectionStrings": {
    "DefaultConnection": "Server=db;Database=shiftsdb;User=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

Ten en cuenta que lo que quiero es armar los Dockerfile y docker-compose con volumenes, para ir trabajando en desarrollo